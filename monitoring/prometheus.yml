global:
  scrape_interval: 15s
  evaluation_interval: 15s

rule_files:
  - /etc/prometheus/query_alerts.yml

scrape_configs:
  - job_name: agentic-rag-api
    metrics_path: /metrics
    static_configs:
      - targets:
          - api:8000
